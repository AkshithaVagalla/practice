x = 2.9
print(round(x)) # rounds the value of x to the nearest integer, which is 3

x = 2.9
print(abs(-2.9)) # returns the absolute value of -2.9, which is 2.9, because the absolute value of a number is always non-negative

#the absolute value f 2.9 is 2.9, because the absolute value of a number is always non-negative

import math
x = 2.9
print(math.ceil(x)) # rounds x up to the nearest integer, which is 3
print(math.floor(x)) # rounds x down to the nearest integer, which is 2
print(math.sqrt(16)) # returns the square root of 16, which is 4.0